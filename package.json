{
  "name": "arena-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "node --test \"tests/**/*.test.js\"",
    "ladder:mock": "node tools/ladder-mock.js",
    "ladder:mock:watch": "node tools/ladder-mock.js --watch",
    "ladder:sync": "node tools/ladder-sync.js",
    "ladder:flags": "node tools/ladder-flags.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.1.0",
    "typescript": "~5.8.0",
    "vite": "^7.0.0"
  }
}
